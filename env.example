# KOAN_ROOT — Path to the koan repository root (required)
# This is auto-set by Makefile targets, but you can override it here.
# Uncomment and set to your actual path:
# KOAN_ROOT=/Users/yourname/koan

# Git identity for koan's commits (optional but recommended)
# KOAN_EMAIL="koan@yourdomain.com"

# =========================================================================
# MESSAGING CONFIGURATION
# =========================================================================

# Messaging provider: "telegram" (default) or "slack"
# KOAN_MESSAGING_PROVIDER=telegram

# Telegram Bot Configuration (default provider)
# Get these from @BotFather on Telegram — see INSTALL.md for instructions.
# KOAN_TELEGRAM_TOKEN=
# KOAN_TELEGRAM_CHAT_ID=

# Slack Configuration (alternative provider)
# KOAN_SLACK_BOT_TOKEN=xoxb-your-bot-token
# KOAN_SLACK_CHANNEL_ID=C01234ABCDE
# KOAN_SLACK_APP_TOKEN=xapp-your-app-token

# =========================================================================
# PROJECT CONFIGURATION (required)
# =========================================================================
# Kōan needs at least one project to work on.

# Option 1: Single project (simpler)
# KOAN_PROJECT_PATH=/Users/yourname/myproject

# Option 2: Multi-project (up to 5 projects)
# Format: name:path;name2:path2
# KOAN_PROJECTS=myapp:/Users/yourname/myapp;backend:/Users/yourname/backend

# =========================================================================
# OPTIONAL: Run configuration
# =========================================================================
# KOAN_MAX_RUNS=20           # Max runs before auto-pause (default: 20)
# KOAN_INTERVAL=5            # Seconds between runs (default: 5)
# KOAN_BRIDGE_INTERVAL=3     # Telegram poll interval in seconds (default: 3)
